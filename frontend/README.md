# Frontend

Next.js recruiting ATS UI with TailwindCSS and local shadcn-style components.

## Run

```powershell
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the FastAPI backend is not running at `http://localhost:8000/api`.
